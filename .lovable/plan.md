

## Replace Third Carousel Image

Copy the uploaded handmade soap image to `src/assets/hero/slide-3.jpg`, replacing the current slide 3 image. The import in `HomePage.tsx` already references this path, so no code changes are needed.

The image is portrait-oriented (roughly 9:16). The carousel uses `object-cover` on a wide container, so the image will be cropped to fill the hero area. This is fine — the basket of soaps is centered and will remain the focal point.

### Steps
1. Copy `user-uploads://0T7A0070.png` to `src/assets/hero/slide-3.jpg` (overwrite existing)

### Files
- **`src/assets/hero/slide-3.jpg`** — replaced with uploaded image

